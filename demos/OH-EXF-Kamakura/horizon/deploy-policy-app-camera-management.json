{
    "label": "deploy-policy-app-camera-management-${EDGE_OWNER}.${EDGE_DEPLOY}-${ARCH}",
    "description": "A policy for app-camera-management ${ARCH}",
    "service": {
        "name": "${EDGE_OWNER}.${EDGE_DEPLOY}.app-camera-management",
        "org": "${HZN_ORG_ID}",
        "arch": "${ARCH}",
        "serviceVersions": [
            {
                "version": "${VERSION_app_camera_management}",
                "priority": {}
            }
        ]
    },
    "properties": [
    ],
    "constraints": [
        "owner == ${EDGE_OWNER}",
        "deploy == ${EDGE_DEPLOY}",
        "app-camera-management == true",
        "openhorizon.arch == ${ARCH}"
    ],
    "userInput": [
    ]
}
